{
  "entities": {
    "Teacher": {
      "title": "Teacher",
      "description": "A teacher's profile and timetable",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Teacher's name"
        },
        "homeroom": {
          "type": "string",
          "description": "Homeroom assigned to the teacher, e.g., '101' for 1st grade 1st class"
        },
        "timetable": {
          "type": "string",
          "description": "JSON stringified timetable data to simplify storage and updates. Maps days to periods to classrooms."
        },
        "updatedAt": {
          "type": "number",
          "description": "Timestamp of the last update"
        }
      },
      "required": ["name", "timetable"]
    },
    "Meal": {
      "title": "Meal",
      "description": "Monthly school lunch menu record",
      "type": "object",
      "properties": {
        "yearMonth": {
          "type": "string",
          "description": "Year and month in YYYY-MM format"
        },
        "title": {
          "type": "string",
          "description": "Title of the monthly meal plan"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of the last update"
        }
      },
      "required": ["yearMonth"]
    },
    "ScheduleAlert": {
      "title": "ScheduleAlert",
      "description": "Push notification record for schedule change or free period",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "message": { "type": "string" },
        "targetTeacher": { "type": "string" },
        "type": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["title", "message", "targetTeacher", "type"]
    }
  },
  "firestore": {
    "teachers/{teacherId}": {
      "schema": {
        "$ref": "#/entities/Teacher"
      },
      "description": "Stores teacher information and their timetables."
    },
    "meals/{mealId}": {
      "schema": {
        "$ref": "#/entities/Meal"
      },
      "description": "Stores monthly school lunch menu records."
    },
    "scheduleAlerts/{alertId}": {
      "schema": {
        "$ref": "#/entities/ScheduleAlert"
      },
      "description": "Stores push notifications for schedule changes and free periods."
    }
  }
}
